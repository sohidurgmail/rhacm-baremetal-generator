apiVersion: v1
kind: Namespace
metadata:
  name: {{ multicluster_engine_managed_cluster_baremetal.cluster_name }}
  labels:
    name: {{ multicluster_engine_managed_cluster_baremetal.cluster_name }}
    cluster.open-cluster-management.io/managedCluster: "{{ multicluster_engine_managed_cluster_baremetal.cluster_name }}"
